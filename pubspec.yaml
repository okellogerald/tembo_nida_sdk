name: tembo_nida_sdk
description: TemboPlus NIDA Authorization Implementation
version: 0.0.1
homepage: https://github.com/okellogerald/tembo_nida_sdk

environment:
  sdk: '>=3.1.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  tembo_ui:
    path: "/Users/mac/Programs/Packages/tembo_ui"

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^2.1.0+1
  build_runner: ^2.2.0
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

